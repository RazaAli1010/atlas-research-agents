# docs/ assets

- **`atlas-demo.gif`** — end-to-end demo recording embedded in the top-level `README.md`
  (New Run → plan approval/edit → live section timeline → streamed report). Drop the
  recording here with exactly this filename to make the README hero render.
